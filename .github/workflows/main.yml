name: "macOS RustDesk - Admin Auto-Login"

on:
  workflow_dispatch:
    inputs:
      RUSTDESK_PASSWORD:
        description: "Set a custom RustDesk password"
        default: "password123"
        required: true

jobs:
  rustdesk:
    name: "🦀 Pure RustDesk"
    runs-on: macos-15

    steps:
      - name: "👤 Create & Activate Admin User"
        run: |
          USERNAME="admin"
          PASSWORD="123456"
          
          # Create user profile
          sudo dscl . -create /Users/$USERNAME
          sudo dscl . -create /Users/$USERNAME UserShell /bin/zsh
          sudo dscl . -create /Users/$USERNAME RealName "Admin User"
          sudo dscl . -create /Users/$USERNAME UniqueID "502"
          sudo dscl . -create /Users/$USERNAME PrimaryGroupID 20
          sudo dscl . -create /Users/$USERNAME NFSHomeDirectory /Users/$USERNAME
          sudo dscl . -passwd /Users/$USERNAME "$PASSWORD"
          
          # Grant Admin rights
          sudo dscl . -append /Groups/admin GroupMembership $USERNAME
          sudo dscl . -append /Groups/wheel GroupMembership $USERNAME
          sudo createhomedir -c -u $USERNAME
          
          # Force the GUI to switch to this user
          # This triggers the login transition in the background
          sudo /System/Library/CoreServices/Menu\ Extras/User.menu/Contents/Resources/CGSession -switchuser 502
          
          echo "✅ Admin user created and session switch triggered."

      - name: "🦀 Install RustDesk"
        run: |
          brew install --cask rustdesk
          echo "✅ RustDesk installed"

      - name: "🖥️ Install Chrome Remote Desktop"
        run: |
          curl -L -o /tmp/crd.dmg https://dl.google.com/chrome-remote-desktop/chromeremotedesktop.dmg
          sudo hdiutil attach /tmp/crd.dmg -nobrowse
          PKG_PATH=$(find /Volumes/Chrome\ Remote\ Desktop* -name "*.pkg" | head -n 1)
          sudo installer -pkg "$PKG_PATH" -target /
          sudo hdiutil detach /Volumes/Chrome\ Remote\ Desktop* -force
          echo "✅ CRD installed"

      - name: "🔓 Grant Permissions (TCC Injection)"
        run: |
          TCC_DB="/Library/Application Support/com.apple.TCC/TCC.db"
          SQL_QUERY="
            INSERT OR REPLACE INTO access (service, client, client_type, auth_value, auth_reason, auth_version, flags) VALUES ('kTCCServiceScreenCapture', 'com.carriez.rustdesk', 0, 2, 4, 1, 0);
            INSERT OR REPLACE INTO access (service, client, client_type, auth_value, auth_reason, auth_version, flags) VALUES ('kTCCServiceAccessibility', 'com.carriez.rustdesk', 0, 2, 4, 1, 0);
          "
          sudo sqlite3 "$TCC_DB" "$SQL_QUERY"
          sudo killall -9 tccd || true
          echo "✅ Permissions injected"

      - name: "🚀 Start RustDesk"
        run: |
          RUSTDESK_APP=$(find /Applications -name "RustDesk.app" -type d | head -1)
          open "$RUSTDESK_APP"
          # Ensure the password is set for the background service
          sudo "$RUSTDESK_APP/Contents/MacOS/rustdesk" --password "${{ github.event.inputs.RUSTDESK_PASSWORD }}"
          echo "✅ RustDesk started"

      - name: "📸 Initial Capture"
        run: |
          echo "⏳ Waiting 25s for ID generation and user switch..."
          sleep 25
          screencapture -x /tmp/rustdesk-id.png
          IMAGE_URL=$(curl -s -F "reqtype=fileupload" -F "fileToUpload=@/tmp/rustdesk-id.png" https://catbox.moe/user/api.php)
          echo "IMAGE_URL=$IMAGE_URL" >> $GITHUB_ENV
          echo "✅ Initial screenshot uploaded"

      - name: "🔄 Connection Loop & Auto-Refresh"
        run: |
          echo "------------------------------------------------------"
          echo "📸 RUSTDESK ID IMAGE: ${{ env.IMAGE_URL }}"
          echo "🔑 RUSTDESK PW: ${{ github.event.inputs.RUSTDESK_PASSWORD }}"
          echo "👤 MACOS USER: admin | PASS: 123456"
          echo "------------------------------------------------------"

          PREVIOUS_STATE="off"
          while true; do
            # Check for active RustDesk TCP connections
            CONN_COUNT=$(netstat -an | grep ESTABLISHED | grep -Ei "\.21116|\.21117" | wc -l)

            if [ "$CONN_COUNT" -gt 0 ]; then
                if [ "$PREVIOUS_STATE" == "off" ]; then
                    echo "🌐 User connected."
                    PREVIOUS_STATE="on"
                fi
            else
                if [ "$PREVIOUS_STATE" == "on" ]; then
                    echo "⚠️ Disconnected. Refreshing..."
                    open -a "RustDesk"
                    sleep 5
                    screencapture -x /tmp/refresh.png
                    NEW_URL=$(curl -s -F "reqtype=fileupload" -F "fileToUpload=@/tmp/refresh.png" https://catbox.moe/user/api.php)
                    echo "📸 NEW ID SCREENSHOT: $NEW_URL"
                    PREVIOUS_STATE="off"
                fi
            fi
            sleep 20
          done
