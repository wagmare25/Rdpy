name: "macOS RustDesk - Direct ID Extraction"

on:
  workflow_dispatch:
    inputs:
      MACOS_VERSION:
        description: "Select macOS Version"
        type: choice
        default: "macos-15"
        options:
          - "macos-15"
          - "macos-14"
      RUSTDESK_PASSWORD:
        description: "Set RustDesk password"
        default: "123456"
        required: true

jobs:
  rustdesk:
    name: "🦀 RustDesk on ${{ github.event.inputs.MACOS_VERSION }}"
    runs-on: ${{ github.event.inputs.MACOS_VERSION }}

    steps:
      - name: "👤 Create Admin User"
        run: |
          USERNAME="admin"
          PASSWORD="123456"
          sudo dscl . -create /Users/$USERNAME
          sudo dscl . -create /Users/$USERNAME UserShell /bin/zsh
          sudo dscl . -create /Users/$USERNAME RealName "Admin User"
          sudo dscl . -create /Users/$USERNAME UniqueID "502"
          sudo dscl . -create /Users/$USERNAME PrimaryGroupID 20
          sudo dscl . -create /Users/$USERNAME NFSHomeDirectory /Users/$USERNAME
          sudo dscl . -passwd /Users/$USERNAME "$PASSWORD"
          sudo dscl . -append /Groups/admin GroupMembership $USERNAME
          sudo dscl . -append /Groups/wheel GroupMembership $USERNAME
          sudo createhomedir -c -u $USERNAME
          echo "✅ Admin user 'admin' created."

      - name: "🦀 Install RustDesk"
        run: |
          brew install --cask rustdesk
          echo "✅ RustDesk installed"

      - name: "🔓 Grant Permissions & Disable Sleep"
        run: |
          sudo systemsetup -setdisplaysleep Off -setsleep Off -setcomputersleep Off > /dev/null 2>&1
          TCC_DB="/Library/Application Support/com.apple.TCC/TCC.db"
          SQL_QUERY="
            INSERT OR REPLACE INTO access (service, client, client_type, auth_value, auth_reason, auth_version, flags) VALUES ('kTCCServiceScreenCapture', 'com.carriez.rustdesk', 0, 2, 4, 1, 0);
            INSERT OR REPLACE INTO access (service, client, client_type, auth_value, auth_reason, auth_version, flags) VALUES ('kTCCServiceAccessibility', 'com.carriez.rustdesk', 0, 2, 4, 1, 0);
          "
          sudo sqlite3 "$TCC_DB" "$SQL_QUERY" || true
          sudo killall -9 tccd || true
          echo "✅ Permissions injected"

      - name: "🚀 Start RustDesk"
        run: |
          RUSTDESK_APP=$(find /Applications -name "RustDesk.app" -type d | head -1)
          open "$RUSTDESK_APP"
          # Apply the password immediately
          sudo "$RUSTDESK_APP/Contents/MacOS/rustdesk" --password "${{ github.event.inputs.RUSTDESK_PASSWORD }}"
          echo "✅ RustDesk started"

      - name: "🆔 Get ID from Config File"
        run: |
          echo "⏳ Waiting 30s for config file to generate..."
          sleep 30
          
          # RustDesk ID is stored in the service config file
          # We search for the 'id' field in the toml/service file
          RD_CONFIG="/var/root/Library/Application Support/RustDesk/config/RustDesk.toml"
          
          if [ -f "$RD_CONFIG" ]; then
             # Extract ID using grep and clean up quotes/spaces
             EXTRACTED_ID=$(sudo grep 'id =' "$RD_CONFIG" | cut -d'=' -f2 | tr -d '" ' || echo "Not Found")
          else
             # Fallback check in the user's home folder if root is empty
             RD_USER_CONFIG="$HOME/Library/Application Support/RustDesk/config/RustDesk.toml"
             EXTRACTED_ID=$(grep 'id =' "$RD_USER_CONFIG" | cut -d'=' -f2 | tr -d '" ' || echo "Manual Check Required")
          fi
          
          # Take a backup screenshot just in case
          screencapture -x ./verify.png
          IMG_URL=$(curl -s -F "reqtype=fileupload" -F "fileToUpload=@./verify.png" https://catbox.moe/user/api.php)
          
          echo "FINAL_ID=$EXTRACTED_ID" >> $GITHUB_ENV
          echo "IMG_URL=$IMG_URL" >> $GITHUB_ENV
          echo "✅ ID Extracted: $EXTRACTED_ID"

      - name: "🔄 Connection Loop"
        run: |
          echo "------------------------------------------------------"
          echo "🆔 RUSTDESK ID: ${{ env.FINAL_ID }}"
          echo "📸 BACKUP IMAGE: ${{ env.IMG_URL }}"
          echo "🔑 RUSTDESK PW: ${{ github.event.inputs.RUSTDESK_PASSWORD }}"
          echo "👤 MACOS ADMIN: admin | PASS: 123456"
          echo "------------------------------------------------------"

          PREVIOUS_STATE="off"
          while true; do
            CONN_COUNT=$(netstat -an | grep ESTABLISHED | grep -Ei "\.21116|\.21117" | wc -l)
            if [ "$CONN_COUNT" -gt 0 ]; then
                [ "$PREVIOUS_STATE" == "off" ] && echo "🌐 Connected." && PREVIOUS_STATE="on"
            else
                if [ "$PREVIOUS_STATE" == "on" ]; then
                    echo "⚠️ Disconnected. Monitoring..."
                    PREVIOUS_STATE="off"
                fi
            fi
            sleep 20
          done
